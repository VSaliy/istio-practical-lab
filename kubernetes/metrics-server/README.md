# metrics-server

Metrics Server is installed from the pinned upstream release bundle in `scripts/install/install-metrics-server.sh`.
The installer applies lab-specific kubelet address and TLS flags after installing the complete upstream resource set.
