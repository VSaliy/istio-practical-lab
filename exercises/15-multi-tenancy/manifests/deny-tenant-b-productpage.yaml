apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: deny-tenant-b
  namespace: bookinfo
spec:
  selector:
    matchLabels:
      app: productpage
  action: DENY
  rules:
    - from:
        - source:
            namespaces:
              - tenant-b
