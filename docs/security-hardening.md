# Security Hardening

Baseline hardening topics:

- strict mTLS by default
- namespace-level default deny (Kubernetes + Istio)
- least-privilege service accounts
- periodic certificate and token rotation
