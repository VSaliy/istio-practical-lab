apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: ratings
  namespace: bookinfo
spec:
  hosts:
  - ratings
  http:
  - timeout: 1s
    route:
    - destination:
        host: ratings
        port:
          number: 9080
