apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: httpbin-egress
  namespace: bookinfo
spec:
  hosts:
  - httpbin.org
  location: MESH_EXTERNAL
  ports:
  - number: 443
    name: https
    protocol: TLS
  resolution: DNS
